name: Compare the execution time

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  Make-comparison:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Install packages
        run: sudo apt-get install libbenchmark-dev libgmp3-dev libcrypto++-dev libcrypto++-doc libcrypto++-utils

      - name: Set reusable strings
        id: strings
        shell: bash
        run: |
          echo "ci=${{ github.workspace }}/ci" >> "$GITHUB_OUTPUT"
          echo "benchmark=${{ github.workspace }}/benchmark" >> "$GITHUB_OUTPUT"
          echo "build=${{ github.workspace }}/benchmark/cmake-build-release" >> "$GITHUB_OUTPUT"

      - name: Configure
        run: >
          cmake -B ${{ steps.strings.outputs.build }}
          -DCMAKE_CXX_COMPILER=g++
          -DCMAKE_C_COMPILER=gcc
          -DCMAKE_BUILD_TYPE=Release
          -S ${{ steps.strings.outputs.benchmark }}

      - name: Build
        run: cmake --build ${{ steps.strings.outputs.build }} --target Benchmark -j8

      - name: Run generated tests
        working-directory: ${{ steps.strings.outputs.build }}
        run: ./Benchmark --benchmark_out=benchmarks.json

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install python packages
        run: |
          python -m pip install --upgrade pip
          pip install matplotlib numpy requests

      - name: Run Python script
        working-directory: ${{ steps.strings.outputs.ci }}
        run: python make_plot.py ${{ steps.strings.outputs.build }}/benchmarks.json graph.png

      - name: Upload a picture
        uses: devicons/public-upload-to-imgur@v2.2.2
        id: imgur_step
        with:
          path: ${{ steps.strings.outputs.ci }}/graph.png
          client_id: ${{ secrets.IMGUR_CLIENT_ID }}

      - name: Update image link
        working-directory: ${{ steps.strings.outputs.ci }}
        run: python update_image_link.py ${{ secrets.DUB_CO_LINK_ID }} ${{ fromJSON(steps.imgur_step.outputs.imgur_urls)[0] }} ${{ secrets.DUB_CO_API_KEY }}


