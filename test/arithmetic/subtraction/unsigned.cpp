#include <gtest/gtest.h>
#include "../../../Aeu.h"
#include "../../../Aeu-Multiprecision.h"
#include "../../benchmarks/benchmarks.h"

TEST(SubtractionUnsigned, Basic) { }

TEST(SubtractionUnsigned, MixedSubtraction) { }

TEST(SubtractionUnsigned, MixedSubtractionAssignment) { }

TEST(SubtractionUnsigned, DifferentPrecision) { }

TEST(SubtractionUnsigned, Huge) { }

TEST(SubtractionUnsigned, HugeAssignment) { }

TEST(SubtractionUnsigned, Decrement) { }